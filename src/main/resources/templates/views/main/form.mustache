<html>
<body>
<div>
    <form action="/greeting/auth" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        <input type="submit" value="Sign Out"/>
    </form>
</div>
<div>
    <form method="post" action="/greeting/main">
        <input type="text" name="text" placeholder="Пожайлуста,введите сообщение"/>
        <input type="text" name="tag" placeholder="Тэг">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        <button type="submit">Добавить</button>
    </form>
</div>
<div>Список ваших сообщений</div>
<form method="post" action="/greeting/filter">
    <input type="text" name="filter">
    <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
    <button type="submit">Найти</button>
</form>
{{#messages}}
<div>
<b>{{id}}}</b>
<span>{{text}}</span>
    <i>{{tag}}</i>
</div>
{{/messages}}
</body>
</html>